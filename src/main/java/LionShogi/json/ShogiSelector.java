package LionShogi.json;

import java.util.HashMap;

/** Interface for interaction for the JSON script parser. */
public interface ShogiSelector
{
	public void onObject(HashMap<String, String> currentObject);
}
